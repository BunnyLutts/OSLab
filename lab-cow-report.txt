Lab COW

这个lab在uvmcopy中实现了COW功能：
不重新分配内存，而是将两个进程的PTE映射到同一个物理页上。
此时应对PTE的flag进行修改：若flag中W位为1，则将其置零（共享页帧不应写入），
同时置COW位为1（标记为COW页帧），此处COW位是RSW位的低位。

程序在写入COW页帧时会触发trap,此时在usertrap()中进行实际的拷贝操作。
copyout中也要实现COW的拷贝操作。
为了正确的释放页帧，需要在kalloc,kfree和uvmcopy中对引用计数进行维护。

难点在于实现细节，边界条件的判断和对不同函数对同一对象如何作用的理解。
